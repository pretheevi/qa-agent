// Acknowledgment: any reply to this email counts. Mail clients auto-preserve the original
// subject (as "Re: <subject>"), and that subject already embeds the report's own date, so
// no special wording is required from the audience (see reminder/remainder.js).
export function ackInstructionsHtml() {
  return `<p style="color:#1565c0;"><strong>To acknowledge this report</strong>, simply reply to this email.</p>`;
}
